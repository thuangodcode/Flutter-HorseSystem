import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF0F0F11);
  static const Color surface = Color(0xFF1B1B1E);
  static const Color surfaceLight = Color(0xFF28282E);

  // Brand Colors
  static const Color primary = Color(0xFFFFB300); // Electric Gold
  static const Color secondary = Color(0xFF00E676); // Neon Green/Live
  static const Color accent = Color(0xFF00E5FF); // Electric Cyan

  // Text Colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9E9EAE);
  static const Color textMuted = Color(0xFF6C6C7D);

  // States
  static const Color error = Color(0xFFFF3D00);
  static const Color success = Color(0xFF00E676);
  static const Color warning = Color(0xFFFF9100);
  static const Color info = Color(0xFF2979FF);
}
